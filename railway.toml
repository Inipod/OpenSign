[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["apps/OpenSign/**", "apps/OpenSignServer/**", "Caddyfile.railway", "scripts/railway-start.sh"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
